# JavaScript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
  
version: 2.1

orbs:
  codecov: circleci/codecov-clojure@0.0.4

executors:
  nodejs:
    docker:
      - image: circleci/node:12.16.0
commands:
  sum: 
    steps:
      - run:
          name: Run folder 1
          command: |
            npm install
            npm test
      - store_artifacts:
          path: ./folder1
          destination: folder1
  minus: 
    steps:
      - run:
          name: Run folder 2
          command: |
            npm install
            npm test
      - store_artifacts:
          path: ./folder2
          destination: folder2
jobs:
  test:
    working_directory: ~/project/sum
    executor: nodejs
    steps:
      - checkout
      - sum
  test2:
    working_directory: ~/minus
    executor: nodejs
    steps:
      - checkout
      - minus
workflows:
  main:
    jobs:
      - test
      - test2